# Context

Who you are and what you're working on. Rendered verbatim into all three harnesses.

This file is optional — delete it and the block disappears from the generated package instead of
leaving a "missing" placeholder.

## Me

- **Role:** <what you do>
- **How I want answers:** <length, language, tone>
- **What I don't have access to:** <so the agent stops suggesting it>

## The work

- **Stack:** <languages, frameworks, hosting>
- **Where things live:** <repo paths — these are per-machine, keep them here not in MEMORY.md>

## Hard rules

Things that must never happen. Be specific — a vague rule is a rule that gets broken.

1. <e.g. never push to production without a green test run>
2. <e.g. never send email without explicit approval in chat>
3. <e.g. secrets live in .env only, never in a committed file>
